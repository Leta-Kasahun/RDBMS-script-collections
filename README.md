<p align="center">
  <img src="https://raw.githubusercontent.com/Leta-Kasahun/assets/main/sql-mysql-oracle-banner.png" alt="MySQL and Oracle SQL Commands - From Basic to Advanced" width="100%"/>
</p>

<h1 align="center">MySQL & Oracle SQL Commands |</h1>

<p align="center">
  💡 Master the art of SQL using both <strong>MySQL</strong> and <strong>Oracle</strong> databases!<br>
  🚀 From basic CRUD to transactions, indexing, procedures, and optimization — all in one place.
</p>

SQL (Structured Query Language) is the cornerstone of working with relational databases. While MySQL is widely adopted for web applications and open-source projects, Oracle offers robust enterprise capabilities, including PL/SQL, RAC, and Data Guard.

This repository provides a side-by-side SQL experience — learn how MySQL and Oracle compare in syntax, features, and behavior.

---

## ⚙️ Setup & Connection

### 🔹 MySQL
- CLI: `mysql -u root -p`
- GUI: MySQL Workbench
- Node.js: `mysql2` or `sequelize`

### 🔸 Oracle
- SQL\*Plus: `sqlplus hr/password@localhost/XEPDB1`
- GUI: Oracle SQL Developer
- Node.js: `oracledb`

---

## ✍️ Basic CRUD Operations

| Operation | MySQL Example | Oracle Example |
|----------|---------------|----------------|
| Create   | `CREATE TABLE users (id INT AUTO_INCREMENT, name VARCHAR(50));` | `CREATE TABLE users (id NUMBER GENERATED BY DEFAULT AS IDENTITY, name VARCHAR2(50));` |
| Insert   | `INSERT INTO users (name) VALUES ('Alice');` | Same syntax |
| Read     | `SELECT * FROM users;` | Same syntax |
| Update   | `UPDATE users SET name='Bob' WHERE id=1;` | Same syntax |
| Delete   | `DELETE FROM users WHERE id=1;` | Same syntax |

---

## 🏗️ DDL & Constraints

- Use `CONSTRAINT` for primary/foreign keys in both.
- Oracle supports `DEFERRABLE INITIALLY DEFERRED` for constraints.
- MySQL auto-increments with `AUTO_INCREMENT`; Oracle uses sequences.

---

## 🔄 Joins & Subqueries

- Both support `INNER`, `LEFT`, `RIGHT`, and `FULL OUTER JOIN`.
- Subqueries work in both but Oracle handles inline views with more optimizer control.

---

## 🗂️ Data Types & Differences

| Data Type | MySQL | Oracle |
|-----------|-------|--------|
| Text      | `VARCHAR`, `TEXT` | `VARCHAR2`, `CLOB` |
| Number    | `INT`, `DECIMAL` | `NUMBER(p,s)` |
| DateTime  | `DATETIME`, `TIMESTAMP` | `DATE`, `TIMESTAMP` |

---

## 📄 Pagination Techniques

- MySQL: `LIMIT 10 OFFSET 20`
- Oracle:
```sql
SELECT * FROM (
  SELECT a.*, ROWNUM rnum FROM (
    SELECT * FROM users ORDER BY id
  ) a WHERE ROWNUM <= 30
) WHERE rnum > 20;
📈 Indexing & Optimization
MySQL: CREATE INDEX idx_name ON users(name);

Oracle: Same, but with bitmap indexes and function-based indexes

🔒 Transactions & Concurrency
Start, Commit, Rollback supported in both

Oracle uses SAVEPOINT, more isolation levels

MySQL with InnoDB supports ACID, but not all engines do

📦 Stored Procedures & Functions
Oracle: Uses PL/SQL

MySQL: Uses DELIMITER to define procedures

Examples included in /procedures/ folder.

👥 Security & User Management
MySQL: GRANT SELECT ON db.* TO 'user'@'localhost';

Oracle: CREATE USER user IDENTIFIED BY password;

🧬 Backup, Restore & Replication
MySQL: mysqldump, binlog, master-slave

Oracle: EXPDP/IMPDP, RMAN, Data Pump, Data Guard

⚙️ Advanced Features
Oracle:

PL/SQL

RAC (Real Application Clusters)

Flashback Queries

Autonomous DB

MySQL:

JSON Columns

Replication Clusters

InnoDB Storage Engine

📌 Best Practices & Performance Tuning
Avoid SELECT *

Use indexing wisely

Normalize tables

Use explain plans

📚 Resources
Oracle SQL Docs

MySQL Reference Manual

GeeksforGeeks Comparison

✅ Contributing
Pull requests are welcome! Feel free to fork and enhance this SQL command series.
